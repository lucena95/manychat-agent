{
  "name": "manychat-agent",
  "version": "1.0.0",
  "description": "Agente para crear flujos ManyChat desde WhatsApp via GHL",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "postinstall": "npx playwright install chromium --with-deps"
  },
  "dependencies": {
    "express": "^4.18.2",
    "playwright": "^1.44.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
