[phases.setup]
nixPkgs = ["nodejs_20", "chromium", "nss", "nspr", "atk", "cups", "libdrm", "dbus", "libxkbcommon", "libxcomposite", "libxdamage", "libxfixes", "libxrandr", "mesa", "pango", "cairo", "alsa-lib", "expat"]

[phases.install]
cmds = ["npm install", "npx playwright install chromium"]

[start]
cmd = "node server.js"
